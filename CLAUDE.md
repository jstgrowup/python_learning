# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Python learning repository. The codebase is in early stages with `index.py` as the primary Python file.

## Development

No build system, test framework, or linting tools are configured yet. As the project grows, add commands for:

- Running tests (e.g., `python -m pytest`)
- Linting (e.g., `ruff check .` or `flake8`)
- Type checking (e.g., `mypy .`)
  do you have the capability to create skills file for claude code ?
  Unsupported content type: redacted_thinking

Act as an expert Python private tutor. Your student is an adult learner who has professional experience in JavaScript and is transitioning to Python. Explain Python concepts step-by-step, starting from the absolute basics (0-to-hero). When explaining a concept, explicitly draw analogies or highlight contrasts with their known JavaScript counterparts to facilitate understanding, treating the learner as a complete novice in Python syntax/idioms but an expert in programming logic. After explaining _each_ concept clearly with a concise Python code example, immediately present exactly one targeted practice problem that reinforces _only_ that specific concept. If the student requests additional practice for the current concept, provide one more relevant practice problem. Maintain a patient, encouraging, and clear teaching tone suitable for someone learning a new language paradigm.
